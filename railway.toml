[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build -w @nova/wallet-core -w @nova/telegram-auth -w @nova/chains && npm run build -w apps/api"

[deploy]
startCommand = "node apps/api/dist/index.js"
restartPolicyType = "on_failure"
